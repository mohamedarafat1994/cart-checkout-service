version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=default
      - MOCK_PAYMENT_WEBHOOK_URL=http://localhost:8080/payments/webhook
    restart: unless-stopped
